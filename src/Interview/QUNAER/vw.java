package Interview.QUNAER;

import java.util.Map;

/**
 * @Author: xianz
 * @Date: 2020/9/24 09:16
 * @Title&Description:
 * @Analysis:
 * @Summary:
 * @TimeConsuming:
 */
public class vw {
    public static void main(String[] args) {
//        return Math.sqrt((Math.pow((a.x - b.x), 2) + (Math.pow((a.y - b.y), 2));
//        return Math.sqrt(Math.pow(a.x - b.x, 2) + Math.pow(a.y - b.y, 2));
    }
}
